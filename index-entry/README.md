# Index entry

The listing for this cart, in the shape
[gen1recomp-mod-index](https://github.com/bryanthaboi/gen1recomp-mod-index)
takes: drop these two files in as `carts/notquiteog@johto_diorama/` and open a
pull request.

Validated against the index's own checker:

```
$ node scripts/validate.mjs carts/notquiteog@johto_diorama
OK — 1 entry checked, 0 warning(s).
```

Not submitted yet — that is a pull request against someone else's repository,
and it is the cart author's call to make.
